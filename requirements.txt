# --- Web / API layer ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
websockets==13.1

# --- Audio I/O ---
# Decoding of compressed/container formats (mp3, m4a, ogg/opus, webm) is done
# by shelling out to the ffmpeg binary (installed at the OS level, see
# Dockerfile) so we don't need heavyweight/fragile codec bindings in Python.
soundfile==0.12.1

# --- ML / inference ---
# CPU-only torch build to keep the image small and avoid requiring a GPU.
# The docker-compose / Dockerfile installs from the official CPU wheel index;
# if you install requirements.txt directly with plain `pip install -r`, pip
# will pull the (larger) CUDA-enabled wheel from PyPI instead, which also
# works, just uses more disk.
torch==2.4.1
transformers==4.44.2
huggingface-hub==0.24.6

scipy==1.14.1
numpy==1.26.4
scikit-learn==1.5.2

# --- Bonus: language / accent identification ---

# --- Observability ---
python-json-logger==2.0.7

# --- Testing / eval harness ---
pytest==8.3.3
httpx==0.27.2
pandas==2.2.3
